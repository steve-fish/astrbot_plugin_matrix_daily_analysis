# 更新日志 (CHANGELOG)

## Unreleased

- 修复话题等 LLM 输出中存在嵌套数组时 JSON 被提前截断的问题，不再破坏有效 JSON 中的引号与中文标点。
- 修正 `llm.retries` 语义：`0` 次重试仍会发起首次请求，并兼容 AstrBot 新版 token usage 字段。
- 将全局历史前缀、用户和 bot 过滤统一应用到群分析、个人报告、对话投票与自动分析。
- 增强 Matrix 历史分页容错，跳过异常事件并避免重复 token 导致的循环和重复消息。
- 将 Matrix 倒序历史统一转换为正序对话后再交给 LLM，个人报告仍优先选取最新消息样本。
- 图片报告改为直接读取本地渲染结果后上传，修复重试队列将文件路径当作二进制数据的问题，并让定时任务正确感知发送失败。
- 清理 PDF 文件名中的 Matrix 冒号、路径分隔符和其他跨平台非法字符，防止自定义格式逃离报告目录，并修复系统 Chromium 被误当作 Edge channel 启动的问题。
- 降低对话投票、个人报告、用户称号和金句分析的默认 token/消息上限，与配置提示及运行时回退值保持一致。

## 0.0.2

新增群报告分析

## 0.0.1

本日志按“当前插件功能与行为”整理，聚焦 Matrix 平台的真实能力与配置结构。

### 核心能力
- 仅支持 Matrix 房间：群聊日常分析、图片/文本/PDF 报告输出。
- 自动分析调度：支持按时触发与并发控制，支持排除指定 bot 的 matrix 账号。
- 报告模板：支持多模板切换与预览，包含图片与 PDF 版本模板。
- LLM 分析：话题/用户称号/金句分析，支持独立 Provider 与提示词配置。

### 输出与发送
- 图片与 PDF 均使用 Matrix 上传接口发送。
- 图片发送失败支持重试队列；重试失败回退文本报告。
- PDF 输出固定保存在插件数据目录的 reports 下，不支持自定义输出目录。

### 配置结构
- 配置按用途分组：`group_access` / `auto_analysis` / `analysis` / `llm` / `output`。
- 分析子模块独立分组（话题/称号/金句），各自的提示词与 Provider 可配置。

### Matrix-only 行为
- 不再包含 QQ/OneBot 相关逻辑或参数（如群名片、合并转发、retcode 分支等）。
- 用户标识以 Matrix 用户 ID 形式保存与展示（字符串）。

---

如需历史版本变更，请以实际仓库版本记录为准。
